server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: colaboradores-service
              uri: http://127.0.0.1:8081
              predicates:
                - Path=/api/v1/colaboradores/**